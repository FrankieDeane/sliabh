# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Metro
.metro-health-check*

# native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# macOS
.DS_Store

# local env files
.env*.local

# logs
npm-debug.*
yarn-debug.*
yarn-error.*

# typescript
*.tsbuildinfo

# nativewind / css-interop generated types
nativewind-env.d.ts
.claude/
.netlify/

# graphify knowledge-graph output (generated locally, no need to version)
graphify-out/

# Native projects are generated from app.json by `expo prebuild`, so they are
# build output, not source. Editing android/ by hand would be silently undone
# by the next prebuild — change app.json or a config plugin instead.
/android
/ios
*.apk
*.aab
